#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@intlify/unplugin-vue-i18n/node_modules/acorn/bin/acorn" "$@"
  ret=$?
else 
  node  "$basedir/../@intlify/unplugin-vue-i18n/node_modules/acorn/bin/acorn" "$@"
  ret=$?
fi
exit $ret
